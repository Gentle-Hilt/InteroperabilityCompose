<resources>
    <!-- Base application theme. -->
    <style name="Theme.Interop" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_appbar</item>
        <item name="colorPrimaryVariant">@color/main_appbar</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_appbar</item>
        <item name="colorSecondaryVariant">@color/main_appbar</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/main_appbar</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Interop.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Interop.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Interop.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="popupMenuBackground">@color/main_background</item>
    </style>


    <style name="Theme.DrawerLayout" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/fade_gray</item>
    </style>
</resources>